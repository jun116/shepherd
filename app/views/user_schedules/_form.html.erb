<%= form_for(@user_schedule) do |f| %>
  <% if @user_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_schedule.errors.count, "error") %> prohibited this user_schedule from being saved:</h2>

      <ul>
      <% @user_schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
